/* ลงทะเบียนผู้ป่วย */
http://localhost/jpr_api/register/register_patient

METHOD  === 'POST'

Body
    {
        "id":"1234567890123",       /* บังคับ  string    (เลขบัตรประชาชน) */
        "ttl":"นางสาว",             /* ไม่บังคับ  string    (คำนำหน้า เช่น นาย,นาง,นางสาว ถ้าไม่มีให้ส่งให้ส่งเป็นค่าว่าง -> "ttl":""  ) */
        "name":"วนิดา",              /* บังคับ  string    (ชื่อผู้ป่วย) */
        "lname":"บัวขาว",            /* บังคับ  string    (นามสกุลผู้ป่วย) */
        "sex":"ญ",                 /* บังคับ  string    (เพศ กรณีเป็น ชาย=ช ถ้าเป็น หญิง=ญ) */
        "born":"1993-07-01",        /* บังคับ  string    (กรณ๊ไม่ทราบให้ส่ง 0000-00-00) */
        "room":"01"                /* บังคับ  string     (รหัสของห้องตรวจ) */
       
    }